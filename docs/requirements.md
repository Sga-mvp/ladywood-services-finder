# Requirements

*To be drafted.*

This document will hold:

- **User stories** — derived from the personas, in the form
  *"As a [persona], I want to [action] so that [outcome]."*
- **MoSCoW analysis** — splitting the user stories into Must / Should / Could / Won't
- **Non-functional requirements** — accessibility, performance, offline behaviour, language support
- **Acceptance criteria** for each Must-have

User stories live here; the UML diagrams in `uml/` derive from them.
